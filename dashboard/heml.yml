apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kubernetes-dashboard
spec:
  project: default
  source:
    chart: kubernetes-dashboard
    repoURL: kubernetes.github.io/dashboard
    targetRevision: 15.9.0
  destination:
    name: "in-cluster"
    namespace: kubernetes-dashboard
